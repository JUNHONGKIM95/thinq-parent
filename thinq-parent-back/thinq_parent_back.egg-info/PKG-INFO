Metadata-Version: 2.4
Name: thinq-parent-back
Version: 0.1.0
Summary: FastAPI backend for thinq-parent
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi<1.0.0,>=0.115.0
Requires-Dist: oracledb<3.0.0,>=2.5.1
Requires-Dist: pydantic-settings<3.0.0,>=2.8.1
Requires-Dist: sqlalchemy<3.0.0,>=2.0.38
Requires-Dist: uvicorn[standard]<1.0.0,>=0.34.0
Provides-Extra: dev
Requires-Dist: httpx<1.0.0,>=0.28.1; extra == "dev"
Requires-Dist: pytest<9.0.0,>=8.3.5; extra == "dev"

# thinq-parent-back

FastAPI backend starter for the `thinq-parent` project.

## Run

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -e .[dev]
uvicorn app.main:app --reload
```

## Local Oracle 11g

The local defaults are stored in `.env`.

- host: `localhost`
- port: `1521`
- service name: `XE`
- username: `lghr`
- password: `12345`

If your Oracle 11g instance uses a SID instead of a service name, set `ORACLE_SID` and clear `ORACLE_SERVICE_NAME`.

`python-oracledb` connects to Oracle 11g through thick mode, so set `ORACLE_CLIENT_LIB_DIR` when Oracle Instant Client is not already available on the system `PATH`.

App settings use the `APP_` prefix to avoid collisions with machine-wide environment variables.

## Test

```powershell
pytest
```
